
# activate RVM when a new shell opens
export PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
export rvm_trust_rvmrcs_flag=1
if [[ -s $HOME/.rvm/scripts/rvm ]] ; then source $HOME/.rvm/scripts/rvm ; fi
